config-version: 2
version: 2
sources:
  - name: tap_eu_ted     # the name we want to reference this source by
    schema: tap_eu_ted   # the schema the raw data was loaded into
    tables:
      - name: documents